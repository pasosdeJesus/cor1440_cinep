<% col = Cor1440Gen::Actividadpf.where(
  'id IN (SELECT DISTINCT actividadpf_id ' + 
  '  FROM cor1440_gen_actividad_actividadpf)') %>
<%= f.input :busactividadpf,
  collection: col.order(:titulo),
  include_blank: true,
  label_method: :titulo,
  value_method: :id,
  label: false,
  selected: Sip::ModeloHelper.poromision(params, :busactividadpf),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => ''
  }
%>
