<% col = Sip::Departamento.where(
  'id IN (SELECT DISTINCT departamento_id ' + 
  '  FROM cor1440_gen_actividad)') %>
<%= f.input :busdepartamento,
  collection: col.where(id_pais: 170),
  include_blank: true,
  label_method: :presenta_nombre,
  value_method: :id,
  label: false,
  selected: Sip::ModeloHelper.poromision(params, :busdepartamento),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => ''
  }
%>
