<p><table border="1">
  <tr>
    <th>Detalle</th>
    <th>Fecha planeada</th>
    <th>Fecha real</th>
    <th>Devoluciones</th>
    <th>Seguimiento</th>
  </tr>
  <% @registro.informenarrativo.each do |i| %>
    <tr>
      <td><%= i.detalle if i.detalle %></td>
      <td><%= i.fechaplaneada_localizada %></td>
      <td><%= i.fechareal_localizada %></td>
      <td><%= i.devoluciones ? 'Si' : 'No' %></td>
      <td><%= i.seguimiento %></td>
    </tr>
  <% end %>
</table></p>
