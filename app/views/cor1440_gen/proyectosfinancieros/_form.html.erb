<%- if current_usuario.nil?  %>
  <li><a href="usuarios/sign_in">Iniciar Sesión</a>
<%- else  %>
  <article>
  <%= simple_form_for @registro, 
    url: modelo_path(@registro),
    html: { } do |f| 
  %>
    <div data-role="content" class="form-inputs">
      <div id="errores">
        <% if f.object.errors.any? %>
          <div id="error_explanation">
            <div class="alert alert-error">
              <!--%= pluralize(f.object.errors.count, "error") %-->.
            </div>
            <ul>
              <% f.object.errors.messages.each do |msg| %>
                <li>* <%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <%= f.error_notification %>
      </div>

      <div role="tabpanel">
        <ul class="nav nav-tabs" role="tablist">
          <li role="presentation" class="active"><a href="#basicos" 
            class="enviarautomatico" aria-controls="basicos" role="tab" 
            data-toggle="tab">Datos básicos</a></li>
          <li role="presentation"><a href="#recursoshumanos" 
            class="enviarautomatico" aria-controls="recursoshumanos" role="tab" 
            data-toggle="tab">Recursos humanos</a></li>
          <li role="presentation"><a href="#recursoseconomicos" 
            class="enviarautomatico" aria-controls="recursoseconomicos" role="tab" 
            data-toggle="tab">Recursos económicos y desembolsos</a></li>
          <li role="presentation"><a href="#informes" 
            class="enviarautomatico" aria-controls="informes" role="tab" 
            data-toggle="tab">Informes y auditorias</a></li>
          <li role="presentation"><a href="#contabilidad" 
            class="enviarautomatico" aria-controls="contabilidad" role="tab" 
            data-toggle="tab">Contabilidad</a></li>
          <li role="presentation"><a href="#datosfinanciador" 
            class="enviarautomatico" aria-controls="datosfinanciador" role="tab" 
             data-toggle="tab">Datos del financiador</a></li>
          <% if can? :manage, Cor1440Gen::Indicadorpf %>
            <li role="presentation"><a href="#marcologico" 
              class="enviarautomatico" aria-controls="marcologico" role="tab" 
              data-toggle="tab">Marco Lógico</a></li>
          <% end %>
          <li role="presentation"><a href="#anexos" 
            class="enviarautomatico" 
            aria-controls="anexos" role="tab" 
            data-toggle="tab">Anexos</a></li>
          <li role="presentation"><a href="#cambios" 
            class="enviarautomatico" 
            aria-controls="cambios" role="tab" 
            data-toggle="tab">Control de Cambios</a></li>
        </ul>
        <div class="tab-content">
          <div role="tabpanel" class="tab-pane active" id="basicos">
            <%= render 'basicos', :f => f %>
          </div> 

          <div role="tabpanel" class="tab-pane" id="recursoshumanos">
            <%= render 'recursoshumanos', :f => f %>
          </div>

          <div role="tabpanel" class="tab-pane" id="recursoseconomicos">
            <%= render 'recursoseconomicos', :f => f %>
          </div>

          <div role="tabpanel" class="tab-pane" id="informes">
            <%= render 'informes', :f => f %>
          </div>

          <div role="tabpanel" class="tab-pane" id="contabilidad">
            <%= render 'contabilidad', :f => f %>
          </div>

          <div role="tabpanel" class="tab-pane" id="datosfinanciador">
            <%= render 'datosfinanciador', :f => f %>
          </div>
          <% if can? :manage, Cor1440Gen::Indicadorpf %>
            <div role="tabpanel" class="tab-pane" id="marcologico">
              <%= render 'marcologico', :f => f %>
            </div>
          <% end %> 
          <div role="tabpanel" class="tab-pane" id="anexos">
            <% if f.object.id.nil? %>
              <p>Antes de agregar anexos se debe guardar el compromiso institucional</p>
            <% else %>
              <%= render 'anexos', :f => f %> 
            <% end %>
          </div>
          <div role="tabpanel" class="tab-pane" id="cambios">
            <%= render 'cambios', :f => f %>
          </div>


        </div> <!-- tab-content -->
      </div> <!-- tabpanel -->
    </div> <!-- form-inputs -->

    <div class="form-actions" style="text-align: center">
      <%= f.button :submit, 'Validar y Guardar', :class => 'btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
        proyectosfinancieros_ruta, :class => 'btn' 
      %>
    </div> <!-- form-action -->
  <% end %> <!-- simple_form_for @proyectofinanciero -->
  </article>
<% end -%> <!-- else -->

