<%= f.input :busrespgp_id,
  collection: ::Usuario.joins(:sip_grupo).
  where('sip_grupo.nombre in (?)', 
        [Ability::GRUPO_COMPROMISOS, Ability::GRUPO_COORDINADORGP]),
  include_blank: true,
  label_method: :presenta_nombre,
  value_method: :id,
  label: false,
  selected: Sip::ModeloHelper.poromision(params, :busrespgp_id),
  input_html: { 
    class: 'chosen-select',
    'data-enviarautomatico' => ''
  }
%>
